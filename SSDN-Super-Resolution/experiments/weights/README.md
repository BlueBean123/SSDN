# Pre-trained Model Weights

This directory is the default location for saving model checkpoints during distributed training. 

When you run the training script (`scripts/train.py`) on your custom datasets, the generated `.pth` checkpoint files will be automatically saved here.