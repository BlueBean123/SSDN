# Evaluation Results

This directory contains the output super-resolved (SR) images generated during the inference phase.

When executing `scripts/test.py`, the network will process the low-resolution (LR) inputs and save the corresponding high-resolution reconstructions into this folder.